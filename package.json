{
  "name": "s3-thumb-server",
  "version": "2.0.0",
  "description": "A small HTTP server for serving thumbnail images from S3",
  "author": "Michael Jackson",
  "license": "MIT",
  "main": "lib",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rimraf lib && babel ./modules -d lib",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "aws-sdk": "^2.2.37",
    "invariant": "^2.2.0",
    "sharp": "^0.13.0"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-preset-es2015": "^6.5.0",
    "rimraf": "^2.5.2"
  }
}
